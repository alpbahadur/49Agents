.beads
.49agents/
.worktrees/
node_modules/
.DS_Store
*.log
.env
.env.local
cloud/data/
dl/
cloud/dl/
cloud/public/*.min.js
extensions/
.claude/

# Dolt database files (added by bd init)
.dolt/
*.db*

# Deploy scripts (contain server credentials)
scripts/

# Telemetry pull notes (contains the admin token)
TELEMETRY-PULL.local.md
*.local.md
telemetry.json
telemetry.csv

# Playwright MCP scratch output (snapshots, console logs, screenshots)
.playwright-mcp/

# node_modules is also symlinked in some worktrees, which the trailing
# slash above does not match
node_modules
